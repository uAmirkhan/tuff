// Параметры тела героя. Единица длины: диаметр тела в покое равен 1.
// Все числа стартовые точки настройки по критериям SPEC-SPAYK.md.
export const TELO = {
  tochek: 16, // точек по кольцу
  radius: 0.5, // радиус кольца в покое
  massaTochki: 0.25,
  radiusTochki: 0.06, // радиус контакта одной точки
  trenie: 1.0, // коэффициент трения тела о поверхность
  // Связи кольца: сосед держит длину, через одного только сглаживает изгиб.
  // Жёсткая связь через одного не даёт кольцу согнуться в щели (спайк, 17.09).
  kolco: { zhestkost: 0.35, kazhdyy: 1 },
  // Связь через одного односторонняя: не даёт кольцу сложиться в шпильку,
  // но не мешает сгибаться до радиуса щели. dolya: минимум как доля хорды покоя.
  kolcoCherezOdnogo: { zhestkost: 0.5, kazhdyy: 1, dolya: 0.45 },
  // Связи через центр: мягкие и редкие в покое, жёсткие и частые при Выбросе.
  centr: { zhestkost: 0.04, kazhdyy: 8 },
  centrVybros: { zhestkost: 0.9, kazhdyy: 1 },
  // Объём: контур тянется к площади покоя. Мягко, чтобы тело сплющивалось при ударе
  // и сжималось в щели, но не складывалось в блин и не выворачивалось.
  obyom: { zhestkost: 0.08, kazhdyy: 1 },
  // Демпфирование среды за такт: доля скорости, которая сохраняется.
  dempfer: 0.995,
  // Вязкость: порог отрыва связи как доля диаметра за такт.
  vyazkostPorogPol: 0.05,
  vyazkostPorogStena: 0.035,
  // Ползание: с Вязкостью прилипшие точки ползут вдоль опоры по вводу, единиц в секунду.
  // Так тело держится на потолке без ввода и лезет по стене с вводом «вверх».
  polzanie: 2.5,
  // Расплав: трение тела в состоянии расплава.
  rasplavTrenie: 0.01,
  // Корка: множители тяжести и массы.
  korkaGravitatsiya: 2.5,
  korkaMassa: 2.0,
  // Движение: добавка скорости за такт на единицу ввода.
  // 0,0025 не протаскивало тело в щель 0,6 даже в Расплаве, 0,005 протаскивает (спайк, 17.09)
  tyagaGorizont: 0.005,
  tyagaVniz: 0.0025,
  tyagaVverh: 0.00125,
  spin: 0.006,
} as const;

export const MIR = {
  shag: 1 / 60, // фиксированный шаг симуляции, секунды
  podshagov: 16, // релаксаций связей и контактов на такт
  gravitatsiya: 9.0, // единиц в секунду за секунду, единица = диаметр тела
  maxSkorost: 0.25, // максимальное смещение точки за такт, доля диаметра
} as const;
